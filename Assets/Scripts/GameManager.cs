using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnManager : MonoBehaviour
{
    // Behavior: Runs the logic of the game. Start of the turn, check flames, apply effect.
    // Take in user input for moving and playing cards. 'End Turn' button, end of turn effects, start next turn.
    public List<Champion> champions;
    public Board board;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
